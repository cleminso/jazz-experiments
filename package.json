{
  "name": "jazz-experiments",
  "private": true,
  "devDependencies": {
    "@vitest/ui": "^3.2.4",
    "happy-dom": "^18.0.1",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@biomejs/biome": "2.1.3"
  },
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run format-and-lint",
    "test": "vitest"
  }
}
